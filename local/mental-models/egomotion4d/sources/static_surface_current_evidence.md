# Static Surface Current Evidence

This is a bounded current-D-derived source for the static-surface mental model.
Only the following current constraints are in scope.

- **D64**: Stop HCR/generalization parameter sweeps. Route 2 must align real-current association, surrogate objective, reassociated algorithm checks and an independent fixed-common product gate. Route 3 requires explicit capacity evidence.
- **D69**: The 100f blocker was product-observer versus optimizer-association effective-domain mismatch, not missing pose or higher surface DoF. `fixed_gated_3d` keeps coverage accounting independent.
- **D72**: One frozen optimizer/config passed DAGE/Pi3X x scene0/4/22 x100f residual gates with unchanged temporal/coverage denominators. This uses per-scene independent GT calibration and does not prove GT-free metric scale or arbitrary-domain promotion.
- **D76**: A canonical gauge is not inherently metric. Source-unit and algorithm-equivariance are separate gates; D70/D72 are metric-reportable only because of independent per-scene GT calibration.
- **D77**: The accepted scalar patch-position mechanism uses whole-patch move-or-abstain so no baseline-valid member is lost. It does not justify joint pose or extra DoF.
- **D79**: Legacy correction groups cannot provide renderable SurfacePatch topology because they exhibit both fragmentation and local chaining. Organized-depth faces own topology; legacy groups provide correction evidence only.
- **D80**: Region-pair rows are correlated audit evidence, not independent constraints. Optimization must use region x canonical-surfel incidence, one output position per frozen vertex, no seam duplication and no transitive closure.
- **D82**: Exact incidence reconciliation is mechanism-positive but read-only and not a heldout, mesh or product claim.
- **D83**: Full-step geometry violates local orientation; the only accepted correction is one global topology-certified dyadic step, never face deletion, per-vertex clamp or local alpha.
- **D84**: The topology-certified geometry shadow preserves all vertices/faces and exact transport but remains diagnostic; it does not establish heldout or product benefit.
- **D85**: Leakage-free heldout D2F gain is only 0.1775%, below the 5% gate. Target fidelity and same-packet residual are not substitutes for heldout benefit.
- **D86**: The 93f full-face RGB PLY is a real visual/representation PASS, not evidence that the D2 objective improves geometry.
- **D87**: Transfer attribution locates lost gain in region aggregation plus coupled magnitude shrinkage and directional cancellation, not support abstention or topology alpha.
- **D88**: Exact-incidence projection shows scalar region translation cannot carry D77 benefit under frozen topology. Scalar weight/alpha/support sweeps are closed.
- **D89**: The next bounded capacity test is a coupled affine-height shared-vertex oracle, not a deformation-graph optimizer. It must pass identity, inactive-zero, exact incidence, D88 nesting, scale equivariance, vertex norm, topology step and frozen heldout gates before implementation.

No D44+ implementation timeline, task state or unlisted route is authority here.
