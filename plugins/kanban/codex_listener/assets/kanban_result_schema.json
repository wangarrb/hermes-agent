{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Hermes Codex Kanban Result",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "status": {
      "type": "string",
      "enum": ["done", "blocked"],
      "description": "done when the task is complete; blocked when human input or recovery is required"
    },
    "summary": {
      "type": "string",
      "description": "One-sentence handoff summary for the kanban run"
    },
    "details": {
      "type": "string",
      "description": "Detailed result suitable for the kanban task result field"
    },
    "metadata": {
      "type": "object",
      "description": "Structured handoff facts for downstream workers",
      "additionalProperties": true
    },
    "created_task_ids": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Real kanban task ids created by Codex, if any"
    },
    "block_reason": {
      "type": "string",
      "description": "Required when status=blocked"
    }
  },
  "required": ["status", "summary", "details", "metadata"]
}
